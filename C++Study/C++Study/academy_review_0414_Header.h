#include <cstdio>		// <stdio.h>
#include <cmath>	// sin(), cos(), tan(), exp(), pow(), log()
#include <cstdlib>	// malloc, free,		rand() 난수 생성
#include <ctime>		// <time.h> 의사 난수 생성#pragma once


int add(int, int);

struct Person
{
	int age;
};